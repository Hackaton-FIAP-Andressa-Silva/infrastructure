# Secrets — NEVER commit real keys
.env
!.env.local

# Terraform state (may contain sensitive data)
.terraform/
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.*.backup
*.tfvars
!*.tfvars.example

# OS
.DS_Store
Thumbs.db

# Logs
*.log
